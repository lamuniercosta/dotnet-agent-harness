# Harness config resolved per-consumer — the example is tracked, the real one is not
/harness.yml

# .NET
bin/
obj/
[Bb]uild/
artifacts/
*.user
*.suo
.vs/

# Test + analysis output
StrykerOutput/
TestResults/
coverage/
*.trx
inspectcode-report.xml

# Spec Kit is a runtime dependency, not vendored — see NOTICE
.specify/

# Tooling
.idea/
.vscode/
*.swp
.DS_Store
Thumbs.db

# Repo-local self-development skill projections. The tracked start-issue
# directories are authored overrides; every other entry is generated.
/.claude/skills/*
!/.claude/skills/start-issue/
!/.claude/skills/start-issue/**
/.agents/skills/*
!/.agents/skills/start-issue/
!/.agents/skills/start-issue/**
/scripts/local/.self-skills-manifest.json

# Route-map advisor log: personal usage evidence, not the authored artifact.
# route-map.json itself IS tracked (it's the durable output of #46).
/scripts/local/route-log.jsonl
